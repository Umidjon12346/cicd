name: Check
on: push
jobs: 
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - nmae: Install
        run : npm ci
      - name: Lint Tekshirish
        run : npm run lint
